# .github/workflows/fetch_arxiv.yml
name: Fetch Daily arXiv Papers

permissions:
  contents: write
  
on:
  schedule:
    - cron: '0 0 * * *'  # 每天午夜 (UTC) 运行
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install arxiv

    - name: Run Python script
      run: |
        # python arxiv_daily_fetcher.py >> content/basics/first_content.md
        echo aaa >> content/basics/first_content.md

    - name: Setup Hugo
      uses: peaceiris/actions-hugo@v2
      with:
        hugo-version: "0.123.8"
        extended: true

    # - name: Set up Node.js
    #   uses: actions/setup-node@v2
    #   with:
    #     node-version: '14'

    # - name: Install Hugo
    #   run: |
    #     npm install -g hugo
    #     hugo version

    # - name: Build the site
    #   run: |
    #     mkdir -p public
    #     cd $GITHUB_WORKSPACE
    #     echo $PWD
    #     ls
    #     hugo -v

    # - name: Deploy to GitHub Pages
    #   uses: peaceiris/actions-gh-pages@v3
    #   with:
    #     github_token: ${{ secrets.GITHUB_TOKEN }}
    #     publish_dir: ./public
        # force_push: true

    - name: Build
      run: hugo --minify

    - name: git_pull
      run: |
        git pull

    - name: Add and commit changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "Github Action"
        git add content/basics/first_content.md
        git add public/*
        git commit -m "Update arXiv papers list"

    - name: Push changes
      id: git_push
      run: |
        # Pull first
        # git pull # --rebase origin "${GITHUB_REF}"
        # Push after
        git push origin "${GITHUB_REF}"
        ls /home/runner/work/github-base/
        ls /home/runner/work/github-base/github-base
        echo 'current dir:' $PWD ${GITHUB_REF}

    - name: Hugo Deploy GitHub Pages
      # You may pin to the exact commit or the version.
      # uses: benmatselby/hugo-deploy-gh-pages@af723bc113338e2d40971769410edbc8966ab417
      uses: benmatselby/hugo-deploy-gh-pages@v2.1.1
      with:
        # If you are defining a custom domain name for your GitHub site, put that value in this variable.
        # cname: # optional
        # Your PAT to authorise the action to do things.
        github_token: ${{ secrets.GITHUB_TOKEN }}
        # The version of Go to install. Go won't be installed if this isn't set.
        # go_version: # optional
        # Any extra arguments to pass to Hugo.
        # hugo_args: # optional
        # If set to `true`, use the *extended* version of hugo.
        # hugo_extended: # optional
        # The publishDir from the hugo config.toml file
        hugo_publish_dir: ./public # optional
        # The version of hugo to use.
        hugo_version: 0.123.8
        # The branch to push the hugo public files, default will be main branch.
        # target_branch: # optional
        # The repo name you want to clone and push to.
        target_repo: github-base
          
          

  # - name: Deploy
  #   uses: peaceiris/actions-gh-pages@v3
  #   if: github.ref == 'refs/heads/main'
  #   with:
  #     github_token: ${{ secrets.GITHUB_TOKEN }}
  #     publish_dir: ./public
